#!/bin/bash

rm -rf gnuplot_temp_file.* mygraph mymath host.log qemu-events.log result \
    result.average result.global result.min result.max results_*.png \
    results_*.log result.*.png result.stddev snapshot-*

